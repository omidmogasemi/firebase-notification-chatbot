{
    "name": "check-firebase-db",
    "version": "0.1.0",
    "scripts": {
        "bootstrap": "lerna bootstrap",
        "build": "lerna run build && lerna run test",
        "test": "lerna run test"
    },
    "private": true,
    "workspaces": [
        "cdk/*"
    ],
    "devDependencies": {
        "lerna": "^4.0.0"
    }
}